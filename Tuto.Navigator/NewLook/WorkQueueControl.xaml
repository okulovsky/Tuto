<UserControl x:Class="Tuto.Navigator.NewLook.WorkQueueControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Expander>
        <Expander.Header>
            <StackPanel Orientation="Horizontal">
                <Label>Tasks</Label>
                <Rectangle Width="20"/>
                <Label Content="{Binding TasksCount}"/>
                <Label>in queue</Label>
                <Rectangle Width="20"/>
                <Label Content="{Binding FailuresCount}" Visibility="{Binding FailuresVisible}"/>
                <Label Visibility="{Binding FailuresVisible}">failed</Label>
            </StackPanel>
        </Expander.Header>
        <ItemsControl ItemsSource="{Binding }"
    </Expander>
</UserControl>
