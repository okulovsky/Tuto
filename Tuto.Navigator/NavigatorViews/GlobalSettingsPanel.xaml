<UserControl x:Class="Tuto.Navigator.GlobalSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:navigator="clr-namespace:Tuto.Navigator"
             d:DesignWidth="500"
             >
    <StackPanel Orientation="Vertical">
        <GroupBox>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition ></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <Label Grid.Column="0" Grid.Row="0" Content="Max distance to silence"></Label>
                <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding VoiceSettings.MaxDistanceToSilence}"></TextBox>

                <Label Grid.Column="0" Grid.Row="1" Content="Silence margin"></Label>
                <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding VoiceSettings.SilenceMargin}"></TextBox>

                <Label Grid.Column="0" Grid.Row="2" Content="Subfolder where the video sumarry should be"></Label>
                <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding RelativeVideoListPath}"></TextBox>

                <Label Grid.Column="0" Grid.Row="3" Content="Enable auto conversion"></Label>
                <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding AutoConversionEnabled}" Grid.Row="3"></CheckBox>

                <Label Grid.Column="0" Grid.Row="4" Content="Enable auto thumbs creation"></Label>
                <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding AutoThumbsEnabled}" Grid.Row="4"></CheckBox>

                <Label Grid.Column="0" Grid.Row="5" Content="Enable auto sound correction"></Label>
                <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding AutoSoundCorrection}" Grid.Row="5"></CheckBox>

                <Label Grid.Column="0" Grid.Row="6" Content="Show processes window"></Label>
                <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding ShowProcesses}" Grid.Row="6"></CheckBox>
            </Grid>
        </GroupBox>
    </StackPanel>
</UserControl>
