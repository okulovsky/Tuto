<UserControl x:Class="Tuto.Navigator.PublishPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:Tuto.Model;assembly=Tuto"
             xmlns:navigator="clr-namespace:Tuto.Navigator"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <!--<StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Command="{Binding Add}">Add</Button>
            <Button Command="{Binding Remove}">Remove</Button>
        </StackPanel>
        <TreeView x:Name="Tree" ItemsSource="{Binding Root}" SelectedValuePath="{Binding SelectedValuePath}" >
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type model:Topic}" ItemsSource="{Binding Items}">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Fill="LightGray"/>
                        <TextBox Text="{Binding Caption}"/>
                    </StackPanel>
                    </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>-->
    </DockPanel>
</UserControl>
